[project]
name = "LLAC"
version = "0.0.0a"

[tool.setuptools.packages.find]
where = ["."]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = [
	"Allocator/Interpreter/tests",
	"Scripts/tests"
]
